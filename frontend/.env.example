# Vercel deployment: leave empty - API is served from the same domain via rewrites.
# Local development: start the backend on port 8000 and uncomment:
# VITE_API_URL=http://localhost:8000
VITE_API_URL=
