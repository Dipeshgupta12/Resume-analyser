# Root requirements.txt for Vercel Python functions
# This includes ALL code from backend/ + Vercel serverless extras

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.25
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
groq>=0.4.0
python-multipart>=0.0.6
pypdf>=4.0.0
python-docx>=1.1.0

# Vercel requires `vercel-python-wsgi` for some setups, but for ASGI (FastAPI)
# we use the built-in ASGI runtime (no extra package needed).